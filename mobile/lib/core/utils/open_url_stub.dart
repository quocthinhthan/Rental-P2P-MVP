// Stub – should never be called directly.
// Conditional imports in open_url.dart resolve to the correct platform file.
Future<void> openUrl(String url) {
  throw UnsupportedError('openUrl not configured for this platform');
}
